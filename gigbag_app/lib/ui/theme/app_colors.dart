import 'package:flutter/material.dart';

class AppColors {
  // Paleta oficial (padronização global)
  static const bg = Color(0xFF1D2428); // Primary Background

  // Background e cards secundários
  static const surface = bg;
  static const surface2 = bg;

  // Bordas (com alpha)
  static const stroke = Color(0x3A848484); // Secondary Base

  /// Secondary Base em texto opaco (#848484), alinhado ao token da paleta.
  static const secondaryBase = Color(0xFF848484);

  // Textos
  static const textPrimary = Color(0xFFFFFFFF); // Primary Text
  static const textSecondary = Color(0xFF95A1AC); // Secondary Text

  // Destaque
  static const accentTeal = Color(0xFF39D2C0); // Destaque
}

